import React from 'react';
import '../styles/Header.scss';

const Footer = () => (
  <header className="header">
    <h1 className="header__title">Recipebook</h1>
  </header>
);

export default Footer;